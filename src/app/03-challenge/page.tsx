import React from "react";
import Modal from "./components/Modal";

/*
 Neste cenário queremos abrir a modal assim que a tela for montada de forma imperativa
*/

const Home: React.FC = () => {
  return <Modal />;
};

export default Home;
